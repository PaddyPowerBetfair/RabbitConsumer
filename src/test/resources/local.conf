amqp {
  connections = [
    {
      ip           = "localhost"
      port         = 5672
      user         = "guest"
      password     = "guest"
      useSSL       = false
      exchangeName = "myExchange"
      queue        = "dwimyQueue"
      routingKey   = ""
      fileName     = "~/output1.json"
    },
    {
      ip           = "localhost"
      port         = 5672
      user         = "guest"
      password     = "guest"
      useSSL       = false
     # exchangeName = "myOtherExchange"
      exchangeName = "myExchange"
      queue        = "myQueue"
      routingKey   = "someQueue"
      fileName     = "~/output2.json"
    }
  ]
}
